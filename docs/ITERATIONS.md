# 迭代台账

> 每轮迭代一段：范围 / 产出 / 测试状态 / 遗留 / 下轮入口。这是跨会话的"进度总线"，配合根目录 `PROGRESS.html`（面向人的扫读视图）使用。

---

## 迭代 5 · 系统界面柚子社化 + 回溯架构重构 · 2026-08-20 · 状态：✅ 完成

- **范围**：用户大规模交互更正——系统界面/控制条/快速栏/确认弹窗/回想重做 + 回溯高效化。
- **产出**：
  - 回溯重构：**锚点+确定性重放**（VM.replayTo；安全点只记 pc，每 16 点一锚点快照，上限 12/320 点）；history 上限 200→400
  - 想起重做：居中宽面板、正序最新在下、每条预留头像位（sprite normal 头部裁切）、选择条目 ❖、⏪ 回溯（确认弹窗）、滚到底下滚关闭
  - 系统界面（SystemMenu）：左页签（画面/声音/文本/操作/其他/存档/读档）+右内容+底栏（返回游戏/主菜单/桌面退出）；设置项 19 项（主音量/粒子密度/空格与右键功能自定义/键位表等）；存读档分页格子（28 槽，页 8）；页签记忆规则（Esc 恢复上次页；设置按钮从存读档跳回画面）
  - 对话框控制条（DockBar）：11 按钮（隐/快存/快读/⏮/◀/AUTO/▶/⏭/⚙/存/读）
  - 右缘快速栏（QuickBar）：hover 弹出、竖排 4/页、翻页、存读切换
  - 风险确认弹窗（ConfirmBox）：快存/快读/覆盖/读档/上下选择肢/回溯/返回标题/退出；默认聚焦"是"，Enter/Esc
  - 空格默认=隐藏对话框（设置可改下一句）；右键=返回上级/打开系统（设置可改隐藏）；H/L/F 键
  - 性能：collectSlots 并行化（系统界面打开 302ms）；CSS transform/opacity 动效
- **测试状态**：单元 34/34 · E2E 11/11（新增系统页签/存读档/控制条/快速栏/滚轮回溯 5 用例）· 金像 2/2 · typecheck/build/预算 ✅
- **遗留**：浏览器无法移动鼠标指针（确认弹窗以默认聚焦替代，已获同意）；M2 起逐项调校用户积累的体验清单。
- **下轮入口**：M2 商业作品功能面（画廊/鉴赏/回想/Staff Roll/主题/i18n）。

## 迭代 4 · 交互重定义 + M1 收尾 · 2026-08-20 · 状态：✅ 完成

- **范围**：按用户实测反馈重定义滚轮/回溯交互，完成 M1 全部剩余项。
- **产出**：
  - 滚轮：下 = 前进（同单击，140ms 节流），上 = 呼出对话记录（面板内滚动放行）
  - 回溯收编进对话记录：每条前 ⏪ 按钮跳回该句/该选择；会话内环缓冲 20 个安全点快照（GameState 深拷贝），跳转后截断
  - 对话记录正序（最新在下、自动滚底）；选择肢所选文本入列（core BacklogEntry 扩展 kind:'choice'）
  - 失焦静音（默认开，设置可关）
  - 性能预算 CI：scripts/check-budget.mjs（JS gzip+HTML ≤3MB，当前 174KB）接入 ci.yml
  - 金像测试框架：e2e/visual.spec.ts 两张基线（文本区 mask，3% 容差）
  - 白皮书 v1.1 同步：§7.4 与附录 B 键位表按新交互修订
- **测试状态**：单元 34/34 ✅ · E2E 6/6 + 金像 2/2 ✅（新增滚轮/回溯/选择记录用例一次通过）· typecheck/build/预算 ✅
- **遗留（M1 完成）**：TTI 预算测量（M3）；金像跨 runner 字体差异观察（CI 暂未跑金像，M2 接入）。
- **下轮入口**：M2 商业作品功能面（CG 画廊 / 音乐鉴赏 / 场景回想 / Staff Roll / 主题系统 / i18n 基础）。

## 迭代 3 · 双 bug 修复 + M1 后半 · 2026-08-20 · 状态：✅ 完成

- **范围**：用户实测反馈的两个 bug + M1 剩余大项。
- **产出**：
  - fix(ui)：选择肢纵向排列（.yg-choice-list 布局缺失导致按钮按 inline-block 横排）
  - fix(ui)：保存模式下自动 A/B/C 与快速槽禁用（系统槽位），读取模式不受限
  - 转场全家：slide（l/r/u/d）/blinds/circle/feather（遮罩动画实现），编译器转场参数语法扩展（slide:l:400 等），兼容 black:1000 颜色简写
  - 立绘待机呼吸（±2.5px 正弦、相位按角色错开）
  - 想起（Backlog）"以前读过"浅色标注（本局开始时的已读基线快照）
  - 存档导出/导入：JSON 文件下载与上传（缩略图 base64 内嵌、按 savedAt 新者胜、已读/解锁进度并集合并、游戏 id 校验）
  - canary 剧本加入 hallway 滑动转场覆盖新语法
- **测试状态**：单元 34/34 ✅ · E2E 5/5 ✅（新增选择肢纵向断言 + 系统槽禁用断言）· typecheck/build ✅
- **遗留（M1 收尾）**：会话内回溯（rollback）、失焦静音设置项、性能预算 CI、金像截图测试框架。
- **下轮入口**：M1 收尾 → M2 商业作品功能面（画廊/音乐鉴赏/场景回想）。


## 迭代 2 · hotfix + M1 切片一 · 2026-08-20 · 状态：✅ 完成

- **范围**：线上 bug 修复（未定义变量崩溃）+ M1 前半：Auto / Skip / 天气粒子。
- **产出**：
  - fix(core)：未定义变量假值语义（数值 0/逻辑假/字符串空），@set 落库 false；编译期警告措辞更新（线上 bug 1 号）
  - Auto 模式：A 键开关、徽标；等待 = 文本完成 + 语音播完 + 基础延迟 + 逐字附加；面板打开自动挂起；手动点击立即前进不退出 Auto
  - Skip：Tab 三态循环（关→仅已读→全部）；仅已读模式遇未读行自动停止；选择肢前停止；Ctrl 按住临时快进；快进时文本瞬时显示、语音静默、wait 压缩（fast 通道）
  - 天气粒子：sakura/snow/rain/fireflies/dust 五预设（程序化贴图、正弦摆动、密度参数、萤火虫呼吸发光），舞台差分接入
  - 设置新增"自动模式等待"滑条
- **测试状态**：单元 34/34 ✅ · E2E 5/5 ✅（新增 Auto 自动推进、Skip 两段语义两个用例）· 樱花粒子截图视觉验证通过。
- **遗留（M1 后半）**：Backlog 完整版（已读变色/上限提示）、会话内回溯、转场全家（slide/blinds/circle/feather）、立绘呼吸、存档导出导入、Firefox 私密模式降级提示、性能预算 CI、金像测试。
- **下轮入口**：M1 后半（按上述清单顺序）。

## 迭代 1 · M0.1–M0.7 · 2026-08-20 · 状态：✅ 完成

- **范围**：M0 全部切片（骨架 / 解析器 / 内核 / 文本窗 / 舞台 / 音频 / 选择肢+存读档+标题 / canary demo）。
- **产出**：
  - 6 个 workspace 包 + canary 游戏工程（`games/demo`，含确定性占位资产生成器：SVG 背景/立绘 + 合成 WAV）
  - 剧本语言 v0.1 全集（对话/旁白/行内标记/17 条命令/if·choice 块/表达式），编译期全量校验（文件:行:列）
  - 内核 VM（跳转/子程序栈/确定性随机/once 选项）、GameState、存档快照/重定位校验
  - DOM 文本窗（预布局打字机/ruby/傍点/变速/停顿/两段式推进）、标题/暂停/设置/存档/回想面板、「藍」主题
  - Pixi v8 舞台（bg/立绘/交叉淡化/黑场转场/聚焦压暗/震屏/闪光/存档截图）、Web Audio 四总线（循环点/交叉/ducking/手势解锁）
  - IndexedDB 存储（失败降级内存）、自动档轮转/快速档/pagehide 自动存档、Vite 虚拟模块管线（`virtual:yanagi-game`，含资源存在性编译校验）
- **测试状态**：单元 34/34 ✅ · typecheck ✅ · `vite build` ✅ · Playwright E2E 3/3 ✅（全流程通关 / 存读档往返 / 设置面板）· 首包产物 dist 1.7MB（预算 3MB 内）。
- **过程中发现并修复的缺陷**（均已回归覆盖或人工验证）：
  1. Pixi v8 渲染器跨 chunk 循环继承导致 `app.init` 永久挂起 → 强制 pixi 单 chunk（D-007）
  2. `publicDir:'assets'` 与 manifest URL 前缀不一致 → 资源 404（D-008）
  3. 输入竞态：loop 过渡期二次按键导致 pc 跳步 + block 悬空死锁 → `busy` 门控（重要设计教训，已固化在 onAdvance）
  4. wait/转场期间点击无效 → 补齐"点击跳过等待"基线行为
- **遗留（M1 处理）**：`@weather/@filter/@fg` 运行期 no-op；Skip/Auto/回溯未实现；已读集未用于跳过判定；转场期间点击不可跳过；文本窗在资源加载期间无"加载中"反馈；天气/滤镜渲染。
- **下轮入口**：M1 玩家体验基线（按白皮书 §14.2 M1 清单顺序：Skip（已读集）→ Auto → Backlog 完整版 → 设置全项 → 存档导出导入 → 转场全家/天气粒子 → 性能预算 CI + 金像测试框架）。
